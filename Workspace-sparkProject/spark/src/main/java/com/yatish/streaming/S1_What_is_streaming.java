package com.yatish.streaming;

public class S1_What_is_streaming {

	/*
	 * What is spark streaming?
	 * Spark will fetch the data from the streaming sources by starting a consumer thread. It keeps on continuosly fetching data from source. we can specify at what interval, spark streaming will
	 * fetch the data. The data that is being fetched in each time interval is called as batch. Every batch gets converted into an RDD and this continous stream of RDDs is represented as DStream.
	 * 
	 * 
	 */
}
